---
title: 'MDX Example Page'
description: 'A sample MDX page in Next.js'
date: '2025-07-29'
---

# Welcome to MDX in Next.js!

This is a sample MDX page that demonstrates various Markdown features.

## Features

- **Markdown** support
- **JSX** components
- **Math expressions** with LaTeX

## Math Example

Here's a mathematical expression:

$$
\int_0^\infty x^2 dx
$$

## Interactive Component

Here's a simple React component embedded in MDX:

```jsx
function Counter() {
  const [count, setCount] = React.useState(0);
  
  return (
    <div style={{ margin: '20px 0', padding: '10px', border: '1px solid #ddd' }}>
      <p>Count: {count}</p>
      <button onClick={() => setCount(count + 1)}>Increment</button>
    </div>
  );
}

<Counter />
```

## Table Example

| Feature | Support |
|---------|---------|
| Markdown | ✅ |
| React Components | ✅ |
| Math | ✅ |
| Syntax Highlighting | ✅ |

## Code Block

```python
def greet(name):
    print(f"Hello, {name}!")

greet("MDX User")
```

## Conclusion

MDX is a powerful way to write rich, interactive content in Markdown with React components!
